pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
               git url : 'https://github.com/GJohannes/baJenkins'
            }
        }
         stage('Build') {
            steps {
                sh 'make' 
            }
        }
    }
}